# UniGame Static ECS Features

Reviewed gameplay features built on Static ECS.

Each feature must be redesigned before implementation with explicit review of:

- convenience and readability;
- performance and hot paths;
- allocation profile;
- Static ECS-native data model;
- demo scene validation.

This package should not contain LeoEcs Proto compatibility wrappers.
